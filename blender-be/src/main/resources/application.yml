spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blender_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 1000MB
      max-request-size: 1500MB

file:
  upload-dir: C:/Users/陈冬烨/Desktop/up

server:
  port: 8080
